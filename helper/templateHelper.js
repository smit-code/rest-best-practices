const fs = require('fs');
const path = require('path');



function sendTemplateFileByNumber(templateNumber) {
    // Assuming template files are named as "template1.txt", "template2.txt", etc.
   
  }